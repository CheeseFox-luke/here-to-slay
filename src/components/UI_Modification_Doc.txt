0. hover立即放大，不要有延迟
1. 操作居中
2. 操作台根据状态显示，包括回合内操作，回合外操作（两种），以及无操作
3. 布局优化
3a 顶栏：主牌堆右上不变；对手左列（前2人）+ 右列（主牌堆下，余下对手）。✓ App.jsx + App.css
3b 底栏：己方 leader/英雄槽居右。✓ App.css
3c 面板固定 party 尺寸（260px）；对手纵列米色底 + gap。✓
3d 底栏左下 = 弃牌堆（缩略展示，与手牌不重叠）。✓ App.jsx + App.css
3f 顶中：怪物牌堆 + 场上怪物并排居中。✓ App.jsx + App.css
3g 中央留白骰子台；AP 徽章左移一 panel 宽（y=50dvh）。✓ ApStatusBadge.css
3e leader 手牌数角标：左上、圆角长方（仿小卡）。✓ CompactPlayerPanel.css
3.5 留出骰子台，以及加入计数器，代替目前的文本判定提示
4. 操作细化，包括cue，具体操作
5. 出牌操作修正，第一次点击是选中，然后激活（出牌）按钮，点击出牌按钮才会出牌
5✓ 统一选中再打出：Play / Challenge / Modify；选中上移；Item 先 Play 再选英雄。App.jsx + ActionConsole.jsx + App.css
6. 出牌动画，首先拍要飞向中间，半透明化，放大，以便骰子在上面运行
7. 弃牌堆随时展开功能


0✓ CardDisplay.jsx：去掉 500ms 延迟，悬停即显示放大预览。
0+ CompactPlayerPanel：leader/英雄 CardDisplay；装备并列小卡；hover 整组放大。✓

1✓ App.jsx + App.css：五项主操作居中（Draw→Play→Slay→Restock→End），Play/Slay 占位，删 Hand 提示行，Debug 靠右。
1+ App.jsx + App.css：action-console 透明浮层；锚定 game-my-section 顶边贴手牌（修 45vh 空隙 bug）。

2✓ App.jsx + ActionConsole.jsx + actionConsoleHelpers：四状态操作台；删右上 Challenge/Modify 条；手牌选中再 Challenge/Modify；attacker 等待文案在操作台。
========== 需求 2 / 操作台 — 已确认设计（实现待你下一轮提问）==========
【产品决定】
- Challenge/Modify 与需求 5 合并：手牌先选中 → 再点操作台 Challenge/Modify 才真正打出（非直接点手牌出牌）。
- Modify 阶段：全员都要能 Pass（与 gameActions.passModifierPhaseWithResult 一致）。
- Challenge 窗：attacker 只显示「等待」，不能 Challenge 自己；其余玩家 Challenge + Pass。
- 删除右上角 modifier-phase-bar / challenge-phase-bar，Pass 等全部迁到 action-console 固定位置。

【操作台状态（当前要做 + 后续扩展）】
- A 回合内：Draw / Play / Slay / Restock / End（仅 mySeat === currentPlayerIndex 且无其它 interrupt）
- B 空闲：非己回合且无响应 → 操作台透明/隐藏
- C Challenge 窗：非 attacker → Challenge + Pass；attacker → 等待（无按钮或仅文案）
- D Modify 窗：全员 → Modify + Pass（已 Pass 则禁用）
- ⚠️ 后续必加（本轮不做详 UI）：选英雄、弃牌、给牌、选目标、Qi Bear、staged 选牌、装备 Item 等 pending* —— 见 isInterruptPhaseActive，需单独状态，勿硬塞进 B。

【已加代码】actionConsoleHelpers.js：handHasPlayableChallenge/Modifier、getPerPlayerChallengeResponse、getPerPlayerModifierResponse、getMy* — 进入 challenge/modify 时按牌局派生，供下一步接操作台。
============================================================

支线任务 1：操作台按钮完全不透明（hover / disabled / primary / placeholder / Debug 均用实底色，opacity:1）；操作台面板背景仍可透明。✓ App.css .action-console .game-actions__btn*

支线任务 2：右上 AP 圆牌（粗框、三行：turn / x/3 / AP）；game.actionPoints；己回合仅紫色字。✓ ApStatusBadge.jsx + App.jsx + App.css

4（部分）actionConsoleHelpers + ActionConsole：旧 statusContent 已删；仅 challengeAttackerWaiting + 英文无牌 cue（NO_*_CARD_HINT）。
